# basic-blog example

This is a minimal Hugo-style blog site used for tsumo development.

## Run

From the repo root:

```bash
../../build/tsumo build --source ./examples/basic-blog
../../build/tsumo server --source ./examples/basic-blog
```
